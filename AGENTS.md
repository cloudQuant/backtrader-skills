# AGENTS.md

`CLAUDE.md` is the canonical agent guide for this repository. Read it first; it covers
environment selection, verification commands, the distribution manifest rebuild rule, the
iteration workflow, and security notes.

Non-Claude hosts (Codex, OpenCode, OpenClaw): treat `CLAUDE.md` as your working agreement too.

Repository maintainers run all Python commands through the Anaconda base environment
(`/Users/yunjinqi/opt/anaconda3/bin/conda run -n base python`). Public README and product
commands remain environment-agnostic.
